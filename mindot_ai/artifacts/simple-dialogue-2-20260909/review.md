# simple-dialogue-2 implementation self-review before live execution

This is Codex's code review, not external GPT approval or evidence of live semantic quality. Active scope: the latest user-adopted prompt MD and package simple-dialogue-2, known revision cbt-known-scope-2, rubric v1.12 hash only. The starting actual 043047Z submission matched all 47 implementation-base entries. Sixty-three current worktree files and the starting diff were preserved before editing. Earlier raw outcomes and source archives remain unchanged.

## Changes and boundaries

Normal write_turn now accepts exactly updates/move/focus/sourceIds. A prior ordinary goal need not be closed before asking another question. NORMAL purpose and route come solely from the selected Move. EXAMPLE/EXPLANATION targets, WAIT gap IDs, safety episode IDs and correction bindings remain real references. GOAL events still update actual gap answers, skips and reopenings. The supplied schema and all five prompt files match byte-for-byte; runtime prompt loading removes one terminal LF as before.

One Provider.messages projection is shared by SELECT, Writer, Assessor, assessment review and Writer repair. First HumanMessage retains full history/context; the current utterance source text appears in the last HumanMessage with sourceId and actual questionCode, while its context metadata points to LATEST_MESSAGE. Last utterance selection is based on the actual public request delta against the prior committed source-address mapping. Cold currentStep, START record sources, multiple changed sources, low-ID source reactivation and explicit no-new-utterance are preserved. Current answered question and unanswered pending question are distinct. Conversation rows contain only public question fields and answerSourceId. The actual Agent call/ToolMessage pair remains paired before the final human utterance on assessment review. Raw memory and original snapshots are not summarized or truncated.

The public facade, real StateGraph, async tools, strict SDK receipt boundary, DTO and atomic commit path remain in use. Simple-dialogue-1 checkpoints migrate with full original snapshot preservation. No new semantic keyword rule, validator, helper LLM, extra tool or user-command draft feature was added. A model-proposed completion remains subject to its existing evidence/review transaction. Backend/frontend were inspected read-only; no public DTO/status changes were made.

## Offline evidence

Twelve existing relationship tests passed through actual public request -> graph -> SDK resource fakes -> tools -> DTO -> commit with a process network-fail guard. They include current last-message binding on all fake calls, low-ID old-answer reactivation, multiple changed answers, cold history, genuine tool pair linkage, ordinary same-turn GOAL answer followed by a different Move, help/corrections, gap semantics, stop/safety, failure atomicity, replay and legacy preservation. There were 27 successful fake product requests, 52 fake generation dispatches and zero live calls. These are plumbing/state tests with scripted model output, not proof of live model decisions. Old internal all-source/coverage requirements are not applied to this minimal core; relevant user function tests remain.

Public OpenAPI matches the preserved starting application (SHA256 27b96486bd1f060dc45dba19d2899a6401fc09870894b82e1cc9ea9fb4f07ba5). The unchanged Python request/response definitions and backend CONFIRM_REQUIRED/SAFETY_STOP consumers remain compatible. Prior read-only consumer inspection found nullable evidence and explicit frontend confirmation/cancel support. This is source inspection and an OpenAPI comparison, not a live browser/backend test.

Representative compact SDK payload maximum: 21,113 o200k_base estimated tokens including 64 framing tokens; 87,011 bytes. The existing 48,000-token/196,608-byte defaults are sufficient and unchanged. Reservation is ceil(estimate*1.75)+phase output cap against the verified model 128,000 context; this is a local estimate, not a provider accounting guarantee. Unknown usage retains its reservation. Phase limits, retry0, timeout30/product180/TTL600 and canary36 generation/12 moderation/1,750,000 cumulative token ceiling remain unchanged.

## Collection review

The current seven known files are verified against the supplied revision manifest. All 170 cases remain in collection. Twenty shared exports cover both versions of the ten fixed MULTIPLE_VALID_ACTIONS cases, preserving nullable scalar expectations, allowedDecisions and assessmentPolicy. Product request projection excludes evaluation metadata. Resolved gold metadata (including nested evaluationContext) fixes the 150 single-action and 15 gold-safety IDs before output; all per-case gold values are in the sidecar. GPT applies its locked rubric diagnostic formulas; Codex does not score or filter cases by observed outputs.

Canary committed responses and acceptance judgments are separate. A parent confirmation is only a candidate for NOT_APPLICABLE_PARENT_CONFIRMATION until its actual substance is reviewed; invalid early confirmation blocks descendants. No pre-send semantic check, fake parent question, forced completion draft or canary retry is allowed. Formal collection uses deterministic mappings, original Q10 source/retries, fixed revised known inputs and one locked runner. Completed cases can be skipped on resume with cumulative budget restoration. A partially collected case without a durable exact runtime checkpoint blocks replay rather than being reconstructed or rerun. Runtime-removal branches are executed only if a genuine next turn exists; otherwise they remain unobserved. Unfavorable outputs stay raw.

The actual iteration log was fetched read-only at start (Drive modified time 2026-09-09T05:25:14.654Z); the latest decisions match this revision. Rubric v1.12 was handled only as raw bytes: 141,290 bytes, SHA256 883fe7c7e1d69f81dd2682ea230899fa1e5030422074fbbb619669c1a2f50390. Its text and release key are excluded from submission. The key and hidden plaintext have not been accessed before the eligibility gate. Source/runner/input/schema/expectations/config are locked before the single live round. Live calls require the already-authorized network-enabled environment because the sandbox previously denied outbound sockets (WinError 10013); no network settings or credentials are changed.

No git commit/push, deployment, shutdown, rubric scoring, ranking or XLSX. Live functional selection and citation reliability remain to be demonstrated by the canary itself. A blocker ends paid progression while independent roots, raw receipts and direct Drive audit submission are completed.
